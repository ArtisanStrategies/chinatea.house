# Core dependencies for chinatea.house static site generator
jinja2>=3.1.0
pydantic>=2.0.0
click>=8.1.0
rich>=13.0.0
xxhash>=3.4.0

# Database
# SQLite is built into Python, no extra dependency needed

# Build optimization
Pillow>=10.0.0  # Image optimization
minify-html>=0.15.0  # HTML minification

# Development
pytest>=7.0.0
pytest-cov>=4.0.0

# Content generation (optional - only needed for AI content)
anthropic>=0.34.0
openai>=1.0.0  # For OpenRouter support

# Google Search Console integration
google-api-python-client>=2.100.0
google-auth>=2.22.0
google-auth-oauthlib>=1.0.0
pyyaml>=6.0.1  # For GSC YAML config

# Indexing & deployment helpers
requests>=2.31.0
matplotlib>=3.7.0
